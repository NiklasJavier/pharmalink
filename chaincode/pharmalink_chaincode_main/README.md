# Beispiel Anwendbarkeit mit
### `initCall`
Initialisiert den Ledger bei dem Start der Anwendung. 

* **Zugriff:** Jeder mit Zertifikat.
* **Parameter:** Keine.
* **Beispielaufruf (Inital, Anwendung startet):**
    ```sh
    '{"function":"initCall","Args":[]}'
    ```